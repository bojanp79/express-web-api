import * as auExpress from "../core/au-express";
import { AuExpressServer } from "../core/au-express-server";
auExpress.bootstrap((server: AuExpressServer) => {

    //server.config.controllersFolder = "/Users/bojanpetkovic/Documents/express-ex/dist/test/controllers";
    //server.app.get('/views', function (req, res) {
      //  res.render('test', { title: 'Hey', message: 'Hello there!' });
    //});
});

//auExpress.bootstrap();̆